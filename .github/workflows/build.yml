name: Java CI with Maven

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment Environment'
        required: true
        type: choice
        options:
          - development
          - staging
          - production
        default: 'development'

jobs:
  build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        java-version: [11]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up JDK ${{ matrix.java-version }}
      uses: actions/setup-java@v4
      with:
        java-version: ${{ matrix.java-version }}
        distribution: 'temurin'
        cache: maven
    
    - name: Build with Maven
      run: mvn -B clean compile
    
    - name: Run tests
      run: mvn -B test
    
    - name: Package application
      run: mvn -B package -DskipTests
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: calculator-jar-jdk-${{ matrix.java-version }}
        path: target/*.jar
        retention-days: 7
    
    # - name: Generate test report
    #   if: always()
    #   run: mvn surefire-report:report
    
    # - name: Publish test results
    #   if: always()
    #   uses: dorny/test-reporter@v1
    #   with:
    #     name: Maven Tests - JDK ${{ matrix.java-version }}
    #     path: target/surefire-reports/*.xml
    #     reporter: java-junit
    #     fail-on-error: false

  print-environment:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'workflow_dispatch'
    
    steps:
    - name: Print Environment Name
      run: |
        echo "=========================================="
        echo "🚀 Deployment Environment: ${{ inputs.environment }}"
        echo "=========================================="
        echo "Pipeline completed successfully for environment: ${{ inputs.environment }}"
